# T48b acceptance

task_id: T48b
status: accepted
reviewer: claude-deep-dot (orchestrator)
date: 2026-08-16

## Review basis

- Read report/validation and the full cli.py/spool.py/hook.py diff.
- Spec conformance: `--ingested-from` on the ingest subcommand only;
  shared validator `^[a-z0-9][a-z0-9_-]{0,31}$`; envelope carries the key
  only when flagged (hook path emits none); drain re-validates at the
  trust boundary and falls back to `source.name` byte-identically when the
  key is absent; tampered token rides the existing
  ValueError -> quarantine + health error path. storage.py untouched.
  hook.py change is signature plumbing only (call sites unchanged in
  behavior).
- Independent re-verification: `make clean && make test` -> 60 tests OK;
  `make clean && make validate` -> 0 fail.
- Four new tests pin: codex attribution end-to-end, invalid CLI token,
  unflagged default unchanged, tampered-envelope quarantine.

## Outcome

T48b accepted. Prerequisite satisfied; T48 resumes under its original
task file with the receiver invoking
`contextdb_cli.py ingest --ingested-from codex`.
