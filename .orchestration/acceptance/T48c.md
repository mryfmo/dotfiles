# T48c acceptance

task_id: T48c
status: accepted
reviewer: claude-deep-dot (orchestrator)
date: 2026-08-16

Generator now emits a conditional `render_managed_paths` helper into
modify scripts only when the managed content contains the chezmoi home
placeholder; the script expands it via `Path.home()` at apply time.
Orchestrator independently piped the deployed deep/standard configs
through the NEW modify scripts: notify renders to the real absolute path
with no residual `{{`; express/review outputs carry no notify and remain
byte-identical. `--check`, validate-agent-assets, and make format all
green on re-run. Source stays machine-portable (no hardcoded user path).
next_action: orchestrator applies the two profile configs and runs E2E-2.
