# T56 acceptance

task_id: T56
status: accepted
reviewer: claude-deep-dot (orchestrator)
date: 2026-08-16

Python staleness detector with dual bounds (10k entries + 5s setitimer),
always-exit-0 contract, runtime-path exclusions (state/spool/health,
sqlite, agmsg db/run/teams), plugin-cache version comparison + managed-root
mtime scan, dedup capped at five `restart recommended:` lines. Doctor
delegates to the installed executable (single source of truth). One async
SessionStart entry appended after herdr (epoch via $(date +%s), the
documented approximation); generator needed no edit and regeneration
changed only the managed settings template. Seven fake-HOME subprocess
tests. Orchestrator re-ran: validate ok (incl. T55 composition), 262 unit
tests OK, format 0, and live-smoked both modes successfully. Worker memory
add verified (d48ba5d1-...). next_action: T57 (asset install manifest).
