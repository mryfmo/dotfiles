# T58 acceptance

task_id: T58
status: accepted
reviewer: claude-deep-dot (orchestrator)
date: 2026-08-16

Remover exceeds the guard spec: all-paths preflight before the first
deletion (prevents partial removal on tampered entries), final-symlink not
followed, fixed-root + manifest-prefix double gate, wholesale-settings
protection, ambiguity refusal (brew/kinds), unknown step exit 1 with known
list. Read-only verified inverses: claude plugin uninstall / codex plugin
remove / herdr integration uninstall; crit has no uninstall -> guarded
path removal documented. Doctor implements the ruled three-class taxonomy
(ACCOUNTED silent / STALE with remove suggestion / ORPHAN manual review),
warnings non-fatal. 13 new tests; orchestrator re-ran: format 0, bash -n
ok, 280 unit tests OK, validate ok, and live-smoked the remover without a
manifest (clean ERROR, no action). Worker memory add verified
(5ed9d1d9-...). Phase 4 (H1) complete.
next_action: T59 (doctor REPAIR reconciliation).
