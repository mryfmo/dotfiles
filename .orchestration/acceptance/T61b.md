# T61b acceptance

task_id: T61b
status: accepted
reviewer: claude-deep-dot (orchestrator)
date: 2026-08-16

Per-identity keys implemented exactly where parameterization exists (one
call site, two runtime entries; ten bare keys untouched); REPAIR now uses
the explicit key identity with a fixed identity-to-tool mapping,
REPLACING command-suffix inference (the simplification the spec asked
for); generic jq key handling meant no new parsers. Manifest integrity:
present-but-invalid -> single ERROR + both manifest scans skipped;
remover invalid-manifest exit 1 pinned. Regression-first evidence; 304
tests OK, shellcheck/format/validate green on orchestrator re-run.
effects=none correct; memory add verified (3e5bb65b-...).
Both bot findings addressed at root cause. next_action: push T61a+T61b to
PR #128, reply to bot comments, rerun CI.
