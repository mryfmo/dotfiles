# T66b acceptance

task_id: T66b
status: accepted
reviewer: claude-deep-dot (orchestrator)
date: 2026-08-17

Deny-first workspace layer implemented beyond spec: read gated alongside
write/edit, symlink-escape and symlink-loop handling, equal-cwd rejection,
invalid/root-cwd fallthrough; send.sh exact-prefix lane with
metacharacter+newline rejection; claude/codex cannot enable the switch
(golden byte-parity kept); policy-order validation added. Orchestrator
live smokes: in-workspace write allow / .. escape ask / send.sh allow /
injection ask — all correct (earlier smoke failures were my own
missing-cwd payloads: schema strictness working as intended). Gates
green. effects=none; memory add (kind=failure) quoted.
next_action: deploy + E2E-π1 rerun.
