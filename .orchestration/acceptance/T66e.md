# T66e acceptance

task_id: T66e
status: accepted
reviewer: claude-deep-dot (orchestrator)
date: 2026-08-17

Single strict resolver for write/edit and sensitive reads: strict=True
cwd+parent realpath, final-component lstat, every OSError family (loop,
missing parent, invalid terminal, root cwd) -> ask; missing final name
remains the intentional creation case. Orchestrator macOS smokes: loop
ask (the CI case), missing-parent ask, clean in-cwd allow. Gates green
(374). effects=none; memory add quoted. next_action: T68c, then push
both to PR #134.
