# T67 acceptance

task_id: T67
status: accepted
reviewer: claude-deep-dot (orchestrator)
date: 2026-08-16

Checker enforces the exact 0.84.2 pin, never touches key material (checks
only the documented env-var NAME per provider), prints ordered RPC
envelope kinds (doubling as the T68 envelope confirmation), and verifies
the permgate extension via --extension startup diagnostics — the correct
pinned-source call, since tool_call handlers do not surface in
get_commands. Operator form covers /login paths, catalog and
thinking-level collection, terms judgment, and future model_profiles.pi
values, with credential recording forbidden. Fake-pi transcripts cover
PASS/SKIP/FAIL and envelope listing. Gates green on re-run.
effects=none; memory add quoted.
next_action: orchestrator chore (pi pin config+lock+install), then the
real headless lane and the operator form handoff.
