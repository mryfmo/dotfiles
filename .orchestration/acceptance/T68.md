# T68 acceptance

task_id: T68
status: accepted
reviewer: claude-deep-dot (orchestrator)
date: 2026-08-17

Bridge delivered per the fixed spec on the empirically confirmed RPC
contracts: scratch-root canonicalization guard, exact-identity join with
the newly legal pi type (join.sh diff purely additive — whitelist + usage
strings; existing types untouched), one persistent rpc child, correlated
prompt delivery with the fixed instruction appended exactly once,
completion on response+agent_end with agent_settled tolerance, inbox
checks only at startup and turn boundaries (no ad-hoc polling), blocked
PONG + nonzero on child death, and NO result-composition path (send.sh
spy pinned). Extension loading is belt-and-braces (--extension fatal
load + global discovery preflight). The model_profiles.pi skip is the
correct call under the trivially-additive clause (values awaited the
operator lane, now known). Gates re-run green (format, shellcheck,
unit-test incl. the new bridge suite, validate). effects=none; memory
add quoted. Phase 4 complete pending T72 E2E-π1.
next_action: T67c, then T69.
