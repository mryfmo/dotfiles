# T68c acceptance

task_id: T68c
status: accepted
reviewer: claude-deep-dot (orchestrator)
date: 2026-08-17

All three P1s fixed at root: (1) both receivers execute only the trusted
installed CLI with --project-root as data (vendored support verified with
paths.py/cli.py citations; orchestrator live smoke: ingest row 64->65
through the fixed codex receiver) — the T48-era same-class hole closed
alongside; (2) identifier grammar at the tool boundary + one safe-quote
helper for all four SQL values in send.sh (orchestrator live smoke: a
DROP-TABLE injection body round-trips byte-exact as data, table intact);
(3) shdoc compliance on the touched comments. Hashes updated; gates and
shellcheck green. effects=none; memory add quoted.
next_action: push T66e+T68c to PR #134, reply to all four bot comments,
rerun CI.
