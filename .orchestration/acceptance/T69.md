# T69 acceptance

task_id: T69
status: accepted
reviewer: claude-deep-dot (orchestrator)
date: 2026-08-17

Elegant mapping choice: the extension emits EXISTING hook event names
(PostToolUse/PostToolUseFailure with canonical tool + primary arg only,
Stop with 240-char text, PostCompact with summary+trigger), so the vendor
normalize layer needs zero new tokens and pi capture lands as first-class
typed events — exceeding the codex turn-granular unknown-typed baseline.
Session-start-cached opt-in check, 5s ingest timeout, all handlers
exception-wrapped, raw tool output deliberately not captured (data
minimization). SHA-256 integrity extended. Gates green on re-run.
effects=none; memory add quoted. Phase 5 (π3) complete pending T72
E2E-π3. next_action: T70 (session evidence extraction).
