# T76b acceptance

task_id: T76b
status: accepted
reviewer: claude-deep-dot (orchestrator)
date: 2026-08-17
cost: n/a

Grammar single-sourced in lib/identifier.sh and enforced pre-mutation at
all three registration boundaries (join/rename/rename-team — inventory
complete) plus send.sh; regex-literal-only-in-helper pinned by test;
parity symlink template generated via the established mechanism.
Orchestrator live smokes: join and rename both reject bad identifiers
with the usage-style error before any state change. Gates green.
effects=none; memory add quoted. next_action: push to PR #136, reply,
merge on green.
