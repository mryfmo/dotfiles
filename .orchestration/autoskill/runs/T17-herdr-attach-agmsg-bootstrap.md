# T17 AutoSkill run

Status: not used.

The task forbids LLM calls. Existing shell, jq, unittest, and repository
patterns were sufficient; the second acceptance revision was handled with a
real-schema regression fixture and learning was triaged manually without
promotion.
