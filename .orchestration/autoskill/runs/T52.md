# T52 Autoskill Record

- `agmsg-orchestration`: followed the T52 task/result contract and reported the shared-worktree status caveat to the orchestrator.
- `understand-anything:understand`: followed the authoritative auto-update prompt, used canonical ignore filtering, deterministic tree-sitter fingerprinting, one targeted file-analyzer batch, lite layer validation, and load-patch-save fingerprints.
- `ponytail` full mode: stopped at `PARTIAL_UPDATE`, preserved the existing architecture/tour, and avoided a full 689-file rebuild when only two files had structural changes.

No new dependency, persistent helper, source abstraction, or full graph rebuild was added.
