# T56 Autoskill Record

- `agmsg-orchestration`: followed T56's edit boundary, five-artifact contract, durable-memory requirement, and T61 live-E2E deferral.
- `python-uv-workflow`: selected stdlib Python after discovery, wrote failing fake-HOME tests first, then used uv for targeted/full tests and generator checks plus Ruff for style.
- `ponytail` full mode: reused the existing generator's verbatim SessionStart flow and made doctor shell the one installed executable; added no second detector, dependency, state database, or harness parser.

No new skill, package, generated-output type, synchronous hook, or internal-state adapter was added.
