# T64 Autoskill Run

- Reused: `agmsg` and `agmsg-orchestration` for the task/result protocol.
- Reused: `python-uv-workflow` for test-first unit validation and uv-based generator checks.
- Reused: `openai-docs` to check for an official public model reference; none was found, so the operator-supplied exact identifier remained authoritative without adding an availability claim.
- Reused: `plan-quality-gate` through its manual fallback because repository entrypoints are absent.
- Reused: `crit-cli` for repository-local, resolved review evidence; no browser review was requested.
- New durable skill: not warranted; the repository generator and validator already own this workflow.
- Ponytail outcome: reused the profile-agnostic generator and added only one validator guard, focused tests, and one rule bullet.

