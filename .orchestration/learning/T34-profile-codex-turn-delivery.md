# T34 learning triage

Candidate reusable rule: when profile-layered configuration owns a state map,
the profile modifier must seed missing base trust entries while preserving
profile-local entries. The task-specific evidence is ready for orchestrator
review; no rule was promoted directly.
