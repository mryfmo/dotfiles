# T40 learning triage

No reusable learn entry was promoted. The committed graph is valid only at its recorded commit, so search-first guidance explicitly compares `.ua/meta.json` with `HEAD` and falls back to grep when stale.
