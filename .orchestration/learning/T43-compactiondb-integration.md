# T43 learning triage

Explicit markers have two compatible forms: a bracket containing exactly one known kind uses post-bracket text as content, while free-text bracket content is itself the memory and may optionally begin with a known kind prefix. Regression tests now preserve both contracts.

Secret-scanner exemptions for vendored security tests should enumerate exact fixture files and document why their dummy credentials are intentional; never exempt a vendor directory broadly.
