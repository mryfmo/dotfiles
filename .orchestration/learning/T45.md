# T45 learning triage

## Reusable learning

Acceptance-time consolidation avoids sharing mutable CompactionDB state across parallel worktrees: worker databases remain worktree-local and disposable, while adopted decisions are explicitly recorded in the main worktree project database.

## Triage

No additional rule candidate was created because this reusable decision is the rule this task directly codified in `compactiondb.md`, `agmsg-orchestration.md`, and the agmsg-orchestration skill. No promotion action was taken.
