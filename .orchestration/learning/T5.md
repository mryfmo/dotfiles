# T5 Learning Triage

## Reusable Learning

No reusable rule candidate was promoted from this task.

## Notes

- Previous validated learn notes advised avoiding Ghostty `initial-command`, but T5 explicitly changed the product decision to use a file-based `herdr-session` entrypoint.
- Treat this as task-specific until the orchestrator accepts the new flow; do not update reusable learn files from this worker task alone.
