# T6 Learning Triage

## Reusable Learning

No reusable learn entry was created.

## Candidate Observation

For modify scripts that merge managed JSON into runtime-owned files, semantic equality should return the original stdin text unchanged. This preserves runtime formatter ordering and whitespace, preventing steady-state `chezmoi diff` churn while still enforcing real managed-value changes.

## Plan Updates

None.
