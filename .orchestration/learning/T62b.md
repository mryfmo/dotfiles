# T62b Learning

Validated failure: the UA auto-update extension filter can omit `.sh` and extensionless executables even though a full analysis supports shell and shebang-driven sources. A current meta hash therefore does not prove graph/fingerprint coverage for these paths.

Apply this to future incremental graph updates by explicitly supplementing changed `.sh` and extensionless executable paths, generating their bundled core fingerprints, and asserting exact path presence in both the graph and fingerprint store before advancing meta.

The extensionless sources receive a valid content fingerprint even when core structural analysis is unavailable; semantic file/function nodes must then be supplied by the file-analyzer procedure.
