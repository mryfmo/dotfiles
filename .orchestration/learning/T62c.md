# T62c Learning

Validated decision: when a UA file node family is advertised as refreshed, its audit must compare the complete source declaration set with the graph function-node set. Applying the usual significance threshold inconsistently can make short but operationally important functions unnavigable.

Apply this to future targeted family refreshes by reporting source count, node count, missing names, extra names, exact line-range parity, and matching `contains` count before advancing the graph. A named bot finding should trigger the family-wide audit rather than a one-node patch.
