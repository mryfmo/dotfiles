# T63 Learning

Validated decision: disposable E2E test-subject sessions are governed by the same model-profile source of truth as persistent sessions. Harness behavior is model-independent, so the regulated `express` profile is the default and throwaway status does not justify ad-hoc `--model` flags.

Apply this to future E2E drivers by sourcing the rendered profile arguments and keeping model IDs confined to `home/dot_agents/agent-config.yaml`.
