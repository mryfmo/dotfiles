# T64 Learning

Validated decision: security-audit work is a distinct model-profile role, not a replacement for the express, standard, review, or deep capability tiers. The canonical manifest can add that role without generator changes because modifier, launcher-env, notify, and apply-time home rendering are profile-agnostic.

Apply this to future profile additions by changing the manifest first, regenerating outputs, and enforcing the intended finite profile set at the asset-validation boundary.

