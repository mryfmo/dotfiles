# T64b Learning

Validated decision: model routing must be audited across both Codex and Claude guidance whenever a new specialized profile is introduced. Security policy descriptions are not routing statements, but broad phrases such as “security-sensitive work” can silently override a narrower specialist-profile rule and must be made unambiguous.

Apply this to future profile changes by searching both rule surfaces for the old role description and recording each routing statement's final profile.

