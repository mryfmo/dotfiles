# T65 Learning

Validated decision: mise config and lock evidence must move atomically. A staged tool skeleton must not create a temporary exception in the global config-lock consistency test; instead, defer the config+lock+install chore and make only the new asset category tolerant of the absent entry while enforcing its exact version once present.

Apply this to future staged tool integrations by separating distributable configuration skeletons from atomic package-manager evidence updates and testing both absent-deferred and present-invalid states.

