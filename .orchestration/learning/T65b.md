# T65b learning triage

## Signals

- Exact pins must select the newest release that has actually aged through the
  repository's seven-day supply-chain cooldown, not simply the newest release.
- A version repin invalidates line citations even when contract text is
  unchanged; immutable tag sources need a fresh semantic and line-anchor audit.

## Disposition

- Decision fact: recorded in CompactionDB as project decision UUID
  `33d84e6b-1ea7-4a36-9281-c80eba2f373d`.
- Implementation: the validator and checker now reject 0.84.2 during cooldown,
  with regression coverage and a complete v0.84.1 anchor table.
- Skill/rule promotion: not proposed. The reusable cooldown rule is already a
  validated repository learning; T65b adds Pi-specific evidence only.

