# T66e learning triage

## Reusable

- Permission paths need strict cwd/parent resolution; non-strict realpath can
  make platform-dependent decisions for loops and missing components.
- Parent resolution and final-component policy are separate: writes reject
  final symlinks, reads strictly follow them for sensitive matching, and only a
  missing write final is allowed.
- Reject terminal traversal names after parent/final decomposition before
  applying lexical containment.

The validated learning is recorded in
`.agents/worklog/codex/learn/20260817_120401_learn.md` and indexed.

## Not promoted

No rule or skill was promoted. One shared resolver and direct adversarial tests
are sufficient.

Failure memory UUID: `3068b7ac-0f52-4bb5-858c-3ebbc4374b60`.
