# T67 learning triage

## Signals

- Task decision: model access has separate headless gating and operator
  subscription-observation lanes.
- Review correction: command output alone is insufficient for a version gate;
  the probe's exit status must be checked before comparing its text.
- Pinned-source finding: `get_commands` proves only registered extension
  commands. For an event-only extension, explicit `--extension` startup plus
  Pi's fatal loader diagnostics provides the reliable load signal.

## Disposition

- Decision fact: recorded in CompactionDB as project decision UUID
  `7ab9f043-a80f-42b0-84a1-89073281b44e`.
- Failure lesson: fixed and regression-tested in T67.
- Skill/rule promotion: not proposed. The extension-load detail is pinned to Pi
  v0.84.2 and belongs with the checker/operator evidence, not a general agent
  rule. Re-evaluate when the Pi pin changes.
