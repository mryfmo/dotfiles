# T67c learning triage

## Signals

- Credential mechanisms are ordered alternatives, not provider categories: an
  env mapping's existence does not imply its variable is configured.
- Check intrinsic availability in precedence order—set env value, exact
  subscription provider key, then safe two-lane SKIP.
- Keep credential handling presence-only and pin both mapped and unmapped
  provider cases so a future branch cannot shadow subscription fallback.

## Disposition

- Failure fact recorded in CompactionDB as project failure UUID
  `632cae23-6d57-4305-83db-a9e964cd20af`.
- Existing repository learn updated with the precedence invariant.
- Rule/skill promotion not proposed; this is checker-specific behavior covered
  by focused fixtures.

