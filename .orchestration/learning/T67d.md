# T67d learning triage

## Signals

- Reasoning output is a typed message variant, not assistant text. Select the
  last text-bearing assistant message rather than assuming the first assistant
  message is the answer.
- Content assertions must not terminate lifecycle parsing. `agent_end`, prompt
  acceptance, answer equality, and extension loading are separate facts.
- A deterministic double-message fixture is enough to reproduce this live
  model behavior without credentials or real model execution.

## Disposition

- Failure fact recorded in CompactionDB as project failure UUID
  `bf0768d8-7484-4778-9074-4697b1767f78`.
- Reusable repository learn added and indexed with the last-text/lifecycle
  invariant.
- Rule/skill promotion not proposed; this checker-specific boundary is pinned
  by focused unit tests.

