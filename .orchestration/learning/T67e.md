# T67e learning triage

## Signals

- An assistant message can complete the turn without text because provider
  failures are represented by `stopReason=error` plus `errorMessage`.
- Lifecycle completion, provider failure, content equality, and extension
  loading are separate facts; diagnostics should preserve that distinction.
- Bound upstream error excerpts before operator output while retaining the
  failure class and enough text to diagnose quota/API failures.

## Disposition

- Failure fact recorded in CompactionDB as project failure UUID
  `0109afd0-a11a-4122-97ef-f65d7782b5eb`.
- Reusable repository learn added and indexed with the error-turn diagnostic
  invariant.
- Rule/skill promotion not proposed; the checker-specific boundary is covered
  by a focused fake-Pi regression.

