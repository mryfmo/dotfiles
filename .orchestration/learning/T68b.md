# T68b learning triage

## Reusable

- Exact shell-prefix lanes are brittle when a model owns quoting/path form.
  Stable privileged operations should be first-class typed tools.
- Pin executable, sender identity, argv boundaries, timeout, and diagnostic
  disclosure in code; expose only payload fields to the model.
- Test both the constrained tool boundary and removal of the former generic
  permission lane.

The validated learning is recorded in
`.agents/worklog/codex/learn/20260817_113250_learn.md` and indexed.

## Not promoted

No rule or skill was promoted. The extension and direct regression tests are
the smallest durable implementation.

Decision memory UUID: `aba142d1-54e6-43cc-88c1-3ada911413ee`.
