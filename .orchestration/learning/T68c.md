# T68c learning triage

## Reusable

- An opt-in path and an executable path are separate trust decisions. A
  repository marker may enable behavior but must never select code to execute.
- Pass project roots explicitly when a runtime's resolution order includes
  inherited environment variables before cwd.
- Model-boundary grammar checks do not replace storage safety: shared legacy
  transports must safely quote every value because non-model callers remain.
- Injection tests should assert exact round-trip data and continued schema
  existence, not only a zero exit code.

The validated learning is recorded in
`.agents/worklog/codex/learn/20260817_125403_learn.md` and indexed.

## Not promoted

No rule or skill was promoted. Two direct trust-boundary regressions and one
small SQLite round-trip test are the smallest durable checks.

Failure memory UUID: `6add9462-a555-407b-817d-28bfe6962c38`.
