# T7 Learning Triage

## Reusable Learning

No reusable learn entry was created.

## Candidate Observation

Ghostty `initial-command` launches a non-interactive login zsh, so command lookup required by that chain must be configured in `.zprofile`, not only `.zshrc`. Interactive-only completion setup can remain in `.zshrc`.

## Plan Updates

None.
