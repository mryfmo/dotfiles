# T70 learning triage

## Reusable

- Pi's persisted JSONL has tree edges but no separate leaf pointer; offline
  active-path tools must mirror reload and choose the last valid appended entry.
- A best-effort JSONL extractor can keep stdout machine-valid by counting bad
  lines in its result while emitting one bounded warning on stderr.
- A size precheck alone has a race; reading at most the limit plus one byte
  preserves the 50 MiB bound if the file grows concurrently.

The validated reusable finding was recorded in
`.agents/worklog/codex/learn/20260817_093833_learn.md` and indexed.

## Not promoted

No orchestration rule or reusable skill was promoted. These details are narrow
Pi session-format semantics and are adequately captured by tests and learn.

Decision memory UUID: `8cb35cf8-96f3-49de-b9ef-efe556ddd0e1`.
