# T74 learning triage

## Reusable

- Partition removals into pure files, surgical mixed hunks, and shared-hunk
  invariants derived from actual commits.
- Preserve a useful security protocol under a neutral name by changing only
  identity surfaces and pinning unaffected callers with byte-exact goldens.
- Reconcile a shrinking test suite numerically to named deleted methods; list
  nested suites and support fixtures separately so coverage loss is explicit.
- Residue scans need a documented filter for forbidden follow-up ownership,
  retained security paths, proper names, and ignored binary caches.

The validated learning is recorded in
`.agents/worklog/codex/learn/20260817_143145_learn.md` and indexed.

## Not promoted

No rule or skill was promoted. The pivot inventory and direct regression tests
are the smallest durable controls.

Decision memory UUID: `e1dbbefb-572a-4d97-a655-901ccc1b0a99`.
