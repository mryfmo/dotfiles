# T8 Learning Triage

## Reusable Learning

No reusable learn entry was created.

## Candidate Observation

Runtime checkers for chezmoi-managed trees need source-to-deployed path normalization for source attribute prefixes such as `executable_` and `private_`. Runtime state directories embedded under managed trees need narrow ignore lists. In mixed-ownership skill roots, top-level deployed skill directories absent from repo source should be warnings, while drift inside repo-managed top-level directories should remain strict.

## Plan Updates

None.
