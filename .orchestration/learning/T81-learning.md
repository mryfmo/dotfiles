# T81 learning triage

Decision: no rule candidate and no new Codex learn file.

The durable convention is recorded directly in the authoritative skill and in
CompactionDB. The implementation uses the existing report and acceptance
artifacts, so it does not reveal a separate reusable engineering lesson.
