# T85 Learning

Incremental UA semantic analyzers can omit still-live short functions under their significance threshold even when those nodes existed in the parent graph. Before classifying an old relationship as endpoint-deleted, consult the bundled structural extractor; if the function still exists, recover the node with its current line range and then restore every surviving-endpoint relationship.

Recorded as project decision memory `20c6f8d5-1bf9-4d00-af75-9ca0e0db04dd` and applied to the T85 node/edge audit.
