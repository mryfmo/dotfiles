# T9 Learning Triage

No new reusable project rule was promoted or proposed.

Existing learn entries used:
- `.agents/worklog/codex/learn/20260708_122200_learn.md`: Herdr `pane split` uses positional pane id; `pane swap` uses `--pane`.
- `.agents/worklog/codex/learn/20260705_135421_learn.md`: Herdr workspace JSON uses `result.workspace.workspace_id` and `result.root_pane.pane_id`.

Task-specific note:
- This was a direct conformance update from stacked/down/up to left/right plus `gpt-5.6-sol`; no broader rule was discovered beyond the existing Herdr CLI learn entries.

