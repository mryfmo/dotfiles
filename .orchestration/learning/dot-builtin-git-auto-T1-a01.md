# dot-builtin-git-auto-T1-a01 learning triage

## Reusable validated knowledge

- Chezmoi 2.70.4 `--use-builtin-git auto` invokes an external `git clone` when Git is discoverable on `PATH` and successfully falls back to built-in Git when the PATH contains no Git.
- A root-created VM `mktemp -d` is mode 0700; make the task-specific directory traversable before running its downloaded executable as a scratch user.

## Promotion

No skill candidate was promoted. The first item is recorded in the project worklog and CompactionDB; the second is a narrow validation detail.
