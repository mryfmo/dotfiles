# dot-residuals-T1-a01 learning triage

No reusable rule candidate was promoted. The only notable task-local correction was to parse `gh extension list` by tab-delimited columns because its display-name field can contain spaces; the production test now preserves that behavior.
