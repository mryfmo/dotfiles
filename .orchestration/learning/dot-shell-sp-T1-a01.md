# dot-shell-sp-T1-a01 learning triage

## Candidate

[memory:decision] Codex `openai-curated` is a reserved authentication-backed catalog. Do not model it as a configurable Git marketplace or call `marketplace upgrade`; attempt the official plugin add and give an authentication/retry instruction when the catalog is unavailable.

[memory:failure] Isolating `HOME` is not enough for mise validation when the process remains in a host-mounted directory containing another mise config. Also change to the scratch user's home before installing locked tools.

[memory:decision] Ubuntu client login-shell convergence belongs in a client-only run-once script after zsh installation; compare the passwd shell first, add zsh to `/etc/shells` only when absent, and call `chsh` once.

## Disposition

- Recorded in the validated Codex learn file and CompactionDB project memory.
- No skill promotion was performed by the worker.
