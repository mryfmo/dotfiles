# Learning
Exact root-relative fixture allowlists do not apply to nested Git checkouts. Detect separate Git trees by ancestor .git entry instead of a particular worktree directory name. Retain ROOT itself and normal file detection. Tests validate file/directory markers and both shared consumers. No promotion.
