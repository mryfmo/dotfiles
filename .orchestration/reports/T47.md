# T47 report

status: ready_for_review
task_id: T47
worker: codex-deep-dot

## Result

[memory: decision — CompactionDB recovery packets are deterministic fixed-order plain-text sections, with ledger-derived sections authoritative over the compact summary and a write/edit-only artifact trail bounded by `recovery.files_budget_chars`.]

- Replaced the opportunistic packet layout with Header, Goal, File modifications, Recent activity, Decisions, Open tasks, Failures, and Compact summary in fixed order. Empty evidence sections render `(none)`.
- Kept the existing evidence disclaimer and verification commands in Header, adding the required ledger-authority sentence.
- Goal uses the first session prompt and newest explicit/current-session decision. Recent activity retains prompt, event, and recent-file line renderings in that order.
- File modifications aggregates every session write/edit path, retains the most recent operation and operation count, orders by latest event, excludes read/search, and drops oldest entries with the required tail when over its sub-budget.
- Decisions retains the hierarchical project-memory projection and adds current-session decisions. Open tasks combines active open-task memories with TaskCreated minus TaskCompleted by task ID.
- Compact summary remains capped at 3000 characters, is labeled reference material, and is always the last section.

All new reads are session-scoped queries inside `recovery.py`. No storage, normalize, hook envelope, CLI, dependency, LLM, or external-process behavior changed.

## Tests

Five new recovery tests plus two extended existing tests cover all seven required cases. The complete vendor suite passed 56 tests, and release validation passed all 10 checks.

This repository is not CompactionDB-opted-in, so no `memory add` command was run.
