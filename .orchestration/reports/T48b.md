# T48b report

status: ready_for_review
task_id: T48b
worker: codex-deep-dot

## Result

[memory: decision — CompactionDB CLI ingestion accepts an optional validated local source token, carries it as `ingested_from` in the spool envelope, and preserves spool-filename attribution exactly when the option is absent.]

- Added `ingest --ingested-from <token>` with shared validation against `^[a-z0-9][a-z0-9_-]{0,31}$`.
- Extended `process_payload` and `spool_event` with an optional keyword argument. Only the CLI passes it; hook callers remain unchanged and emit no provenance key.
- Added the optional `ingested_from` envelope sibling and validated it again at the spool trust boundary.
- `drain_spool` passes a valid explicit source to the existing `ContextStore.insert_event`; absent keys still use `source.name` exactly as before.
- Invalid/tampered envelope sources follow the existing schema-violation path: quarantine plus health error log.
- Documented the trusted-local-input model and added the 2.0.0+dotfiles.4 changelog entry.
- Regenerated `MANIFEST.sha256`. `storage.py` was not changed.

## Tests

Four focused behaviors are pinned across two new CLI tests and two new spool tests: explicit `codex` attribution, invalid CLI token rejection, unchanged unflagged filename attribution, and tampered-token quarantine/error logging.

The complete suite passed 60 tests. Release validation passed all 10 checks.

This repository is not CompactionDB-opted-in, so no `memory add` command was run.
