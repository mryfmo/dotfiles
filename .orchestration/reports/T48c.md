# T48c Report

Status: `ready_for_review`

The generated Codex profile modify scripts now render the portable `{{ .chezmoi.homeDir }}` placeholder with `Path.home()` at apply time, before `MANAGED` is split and merged.

- `home/dot_agents/agent-config.yaml` remains unchanged by T48c and keeps the portable placeholder.
- `scripts/generate-agent-configs.py` emits one `render_managed_paths` helper only for profiles whose managed content contains the placeholder.
- Regeneration updated `modify_deep.config.toml` and `modify_standard.config.toml`.
- `modify_express.config.toml` and `modify_review.config.toml` remain byte-identical and emit no `notify` setting.
- No absolute user path was added to source.

All specified generation, live-input rendering, asset validation, formatting, and diff checks passed. See `.orchestration/validation/T48c.txt`.

[memory: failure — chezmoi modify scripts are executed rather than template-rendered, so a literal `{{ .chezmoi.homeDir }}` inside generated `MANAGED` content reaches the deployed Codex profile unchanged unless the modify script renders it itself.]

[memory: decision — preserve portable placeholders in the manifest and resolve them inside generated Codex modify scripts at apply time with `Path.home()` before merging managed content.]
