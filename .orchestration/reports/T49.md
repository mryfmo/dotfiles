# T49 Report

Status: `ready_for_review`

Implemented `contextdb probe --session <id> [--json]` as a deterministic, LLM-free, read-only evaluation input generator.

- `probe.py` produces recall, artifact, decision, and continuation probes in fixed order and skips types without a non-empty source.
- Recall selects the first `PostToolUseFailure`, falling back to the first event classified with `success=0`.
- Artifact ground truth reuses T47's `_modified_files` helper and is byte-equal to the recovery packet's untruncated File modifications body.
- Decision ground truth includes active `kind=decision` memories visible to the requested session: that session plus project scope.
- Continuation mirrors T47's open-task derivation: visible `open_task` memories plus `TaskCreated` minus `TaskCompleted`.
- CLI wiring bypasses spool draining and opens the existing store with `initialize=False`, so probe execution issues no write statement. A regression test pins the database SHA-256 before and after the CLI run.
- JSON uses a non-empty string `ground_truth` for every probe, as approved by the orchestrator.
- README and operations documentation define usage and keep grading outside the CLI: review profile, low effort, separate context, only when recovery logic changes.

Validation: 67 tests passed; release validation passed 10/10; all 66 manifest entries passed SHA-256 verification.

[memory: decision — `contextdb probe` deterministically generates recall/artifact/decision/continuation questions with ledger-derived string ground truth; grading remains an orchestrator-side review-profile operation and never runs in the CLI.]

Per the task contract, no `memory add` command was run.

