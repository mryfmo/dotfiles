# T5 Report

## Status

ready_for_review

## Changes

- Added `home/dot_local/bin/common/executable_herdr-session` as a bash entrypoint that runs `herdr-agents "$PWD"` with stdout/stderr appended to `~/.config/herdr/herdr-agents.log`, warns on failure, then `exec`s `herdr`.
- Updated `home/dot_zshrc` so bare `herdr` inside Ghostty delegates to `herdr-session`; argumented calls still use `command herdr "$@"`.
- Added Ghostty `initial-command = /bin/zsh -lc 'exec herdr-session'`.
- Added the docs mirror page for `herdr-session`.
- Updated `tests/unit/test_herdr_agents.py` for the new entrypoint, logging behavior, failure attach behavior, argument rejection, Ghostty config, and zsh delegate path.
- Updated the README section that contradicted the new `initial-command` flow.

## Notes

- Did not modify `home/dot_local/bin/common/executable_herdr-agents`.
- Did not run `herdr-session` or `herdr-agents` against real Herdr; unit tests use fake commands on `PATH`.
- Did not run forbidden actions: no commit, push, `chezmoi apply`, `make update`, `make require-crit-review`, or Herdr pane/workspace operations.
