# T55 Report

Status: `ready_for_review`

`validate-agent-assets.py` now has one `hook composition` category covering the three managed hook sources.

- Parses `home/.chezmoitemplates/claude-settings-managed.json` and `vendor/compactiondb/.claude/settings.fragment.json` with `json`, and the Codex hooks section with stdlib `tomllib`, after the validator's existing `render_template_text()` tolerance for chezmoi placeholders.
- Builds a stable per-`(source, event)` inventory of command hooks. The command identity is `command + args`, which distinguishes the two CompactionDB `python3` SessionStart invocations.
- Reports each duplicate command, non-first permgate, synchronous timeout total above `SYNC_TIMEOUT_BUDGET_S = 30`, and missing/reordered expected SessionStart command substring as a separate finding.
- Pins the current SessionStart relative sequences in a PLAN H3-cited constant: Claude `herdr-agent-state.sh`; Codex no managed SessionStart sequence; CompactionDB `contextdb_hook.py` before `contextdb_recover.py`.
- Leaves all existing validator categories and every hook definition unchanged; `main()` only invokes the new category.
- Extended `tests/unit/test_validate_agent_assets.py` because it already loads this validator against isolated temporary roots. Five new tests cover one passing fixture copied from all three real managed sources plus one violating fixture for each rule.

Runtime-generated `.claude/settings.local.json` and `.codex/hooks.json` are intentionally excluded: they are delivery/runtime artifacts owned by `delivery.sh`, not declarative managed sources suitable for a repository-static CI invariant.

The category was run successfully on the current tree before tests were written, as required. Final validation passed 255 unit tests, `make validate-agent-assets`, `make format`, Ruff, and `git diff --check`. Direct category runtime was `0.000936s`, below the 1-second budget.

Durable project decision command executed successfully (memory id `9660682a-f328-4c4a-8f79-d25e987d5262`):

```sh
python3 .claude/hooks/contextdb_cli.py memory add --kind decision --scope project --content "Hook composition across the managed Claude/Codex settings is a validated invariant: no duplicate command per event, permgate first on PermissionRequest, per-event synchronous timeout budget <= 30s, and the intended SessionStart ordering is pinned; violations fail validate-agent-assets."
```

[memory: decision — Hook composition across the managed Claude/Codex settings is a validated invariant: no duplicate command per event, permgate first on PermissionRequest, per-event synchronous timeout budget <= 30s, and the intended SessionStart ordering is pinned; violations fail validate-agent-assets.]

No commit, push, `chezmoi apply`, Bats run, dependency change, hook-source change, or existing validation relaxation was performed.
