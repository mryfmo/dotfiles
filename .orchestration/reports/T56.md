# T56 Report

Status: `ready_for_review`

Sessions can now check public managed-asset timestamps and recommend a restart when installed assets are newer than a supplied session epoch.

- Discovery found versioned public cache directories such as `~/.claude/plugins/cache/ponytail/ponytail/{4.8.4,4.9.0}`, `crit/crit/1.8.*`, and `claude-plugins-official/superpowers/{6.2.0,6.3.0}`. The managed mtime roots are present at `~/.agents/{compactiondb,skills,plugins}`; rendered settings are `~/.claude/settings.json` and `~/.codex/*.toml`.
- Chose Python because stdlib `os.scandir`, monotonic deadlines, `signal.setitimer`, natural version comparison, and the existing Python doctor provide bounded cross-platform macOS/Linux behavior without dependencies or shell-specific `find/stat` syntax.
- `agent-session-staleness check --since <epoch>` detects the highest installed plugin-cache version when it postdates the session baseline, plus newer files in the three managed roots and rendered settings. Results are deduplicated by asset root and capped at five `restart recommended:` lines.
- No-argument mode prints the ten most recent public managed-asset updates with UTC timestamps for doctor/debug use.
- The scan has both a 10,000-entry bound per scan and a process-wide 5-second alarm. Internal failures return exit 0, leave stdout empty, and may emit one stderr line.
- Excludes `state`, `spool`, `health`, SQLite patterns, caches/venvs, and agmsg runtime `db`/`run`/`teams` paths so mutable runtime data does not create false restart recommendations.
- Does not parse harness session state or undocumented files. Detection is explicitly an mtime/plugin-cache approximation.
- Added `check-agent-runtime.py --session-staleness [epoch]`, which delegates to the installed executable. With an epoch it invokes `check --since`; without one it invokes debug history mode. No detection logic is duplicated.
- Appended exactly one async, five-second Claude SessionStart entry after the existing herdr entry. Claude's documented hook payload does not provide a session-start epoch here, so the command passes hook-invocation time via `$(date +%s)`, which is the SessionStart approximation required by the task.
- The existing generator already copies `claude.hooks.session_start` verbatim; `scripts/generate-agent-configs.py` required no edit. Regeneration changed only `home/.chezmoitemplates/claude-settings-managed.json`.
- Added a dedicated `tests/unit/test_agent_session_staleness.py` because the standalone executable and doctor delegation need fake-HOME subprocess fixtures. Seven tests cover clean, detected, failure-isolated, bounded, debug-history, runtime exclusion, and doctor delegation behavior.

Validation passed 262 unit tests, `make validate-agent-assets` including T55 composition, `make format`, Ruff, generator sync, and fake-HOME transcripts. Live installed-session verification remains assigned to T61 E2E-2'.

Durable project decision command executed successfully (memory id `d48ba5d1-4395-4820-9fe3-7b4dff8d33da`):

```sh
python3 .claude/hooks/contextdb_cli.py memory add --kind decision --scope project --content "Sessions detect their own staleness: an async SessionStart hook and a doctor mode compare managed-asset update times against session start and print a restart recommendation; detection is an approximation from mtimes/plugin-cache versions, never from harness internals."
```

[memory: decision — Sessions detect their own staleness: an async SessionStart hook and a doctor mode compare managed-asset update times against session start and print a restart recommendation; detection is an approximation from mtimes/plugin-cache versions, never from harness internals.]

No commit, push, `chezmoi apply`, Bats run, dependency change, harness-internal state parse, synchronous hook registration, or existing hook reorder was performed.
