# T6 Report

## Status

ready_for_review

## Changes

- Added generated `home/.chezmoitemplates/claude-settings-managed.json` as the managed Claude settings baseline.
- Added executable `home/dot_claude/modify_private_settings.json` merge script.
- Deleted `home/dot_claude/private_settings.json`.
- Updated Claude settings references in:
  - `scripts/check-agent-runtime.py`
  - `scripts/validate-agent-assets.py`
  - `tests/unit/test_generate_agent_configs.py`
  - `tests/install/common/lifecycle.bats`
  - `README.md`
  - `home/dot_agents/agent-config.yaml`
  - `home/dot_agents/README.md`
- Added `tests/unit/test_claude_settings_merge.py`.

## Rev2 Resolution

`merge_settings()` still computes managed-key enforcement, but `main()` now writes the original stdin text unchanged when the merged result is semantically equal to the parsed current JSON. This preserves Herdr/Claude's deployed key ordering and whitespace in steady state, eliminating pure ordering churn.

Added tests for:

- reordered-but-equal current JSON passes through byte-identical;
- a real managed value change is still re-dumped and enforced.

## Validation Summary

- `uv run python -m unittest discover -s tests/unit -v`: pass, 71 tests.
- `make validate-agent-assets`: pass.
- Real deployed-file merge simulation: pass; preserves `enabledPlugins` with `crit@crit` and `ponytail@ponytail`; second merge is byte-identical.
- `python3 -m py_compile ...`: pass.
- `git status --porcelain`: only expected source and artifact changes.

## Forbidden Actions

Did not run git commit, git push, chezmoi apply, make update/upgrade, make require-crit-review, bats, direct `~/.claude` edits, or Herdr operations.
