# T60 Report

Status: `ready_for_review`

`effects=none`

This task is the first explicit no-effect example: it changed only allowed files inside the repository working tree and created no persistent global install, `$HOME` write, or external service registration. The required CompactionDB decision is stored in the project database and is not an out-of-repository effect.

## Optional RESULT clause

The source `agmsg-orchestration` skill now documents an additive optional field:

```text
effects=<semicolon-list-of-short-ids>
```

The existing required `AGMSG-RESULT v1` field block remains unchanged, preserving backward compatibility. A task includes `effects` only when it created persistent side effects outside the repository working tree, including global asset installs, `$HOME` writes, or external service registrations. In-repository edits within `allowed_files` are explicitly excluded.

For every declared short ID, the report must state one reverse mapping:

1. A named `~/.agents/.installed-manifest.json` step removable with `remove-agent-asset`.
2. A documented removal procedure.
3. An `irreversible:` statement with rationale.

The Orchestrator Playbook now verifies each mapping before acceptance and records irreversible effects in the acceptance note. The Claude rule adds exactly one matching English bullet.

## Consistency

- T58 semantics: manifest step names are the removal authority consumed by dry-run-first `remove-agent-asset`; the clause does not claim automatic deletion.
- T45 semantics: the existing CompactionDB marker and `memory add` requirements are untouched.
- Phase 6 plan: optional persistent-effect declaration, reverse-mapping location, allowed-files exclusion, and acceptance verification match lines 267-276.
- H6 analysis: the RESULT coeffect declaration and H1 reverse-map verification match the H6 table entry.

The full product diff is four additions/one renumbered line in the skill and one added rule bullet. `make validate-agent-assets`, four-way consistency search, `git diff --check`, scope checks, and the local Crit receipt gate passed.

Durable project decision command executed successfully (memory id `61c32e49-81ec-44c0-8bdb-5bd70d527b92`):

```sh
python3 .claude/hooks/contextdb_cli.py memory add --kind decision --scope project --content "AGMSG RESULTs for tasks with persistent out-of-repo side effects declare them in an optional effects field, and the report states each effect's reverse mapping (installed-manifest step, removal procedure, or explicitly irreversible); the orchestrator verifies this at acceptance."
```

[memory: decision — AGMSG RESULTs for tasks with persistent out-of-repo side effects declare them in an optional effects field, and the report states each effect's reverse mapping (installed-manifest step, removal procedure, or explicitly irreversible); the orchestrator verifies this at acceptance.]

No code, symlink template, dependency, external asset, real HOME, service registration, commit, push, `chezmoi apply`, or Bats run was touched.
