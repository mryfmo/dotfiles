# T61b Report

Status: `ready_for_review`

`effects=none`

Both actionable bot findings on PR [#128](https://github.com/mryfmo/dotfiles/pull/128) are fixed at their producer/consumer boundaries.

## Fixes

- `ensure_mise_npm_agent_cli` now records `ensure_mise_npm_agent_cli:<identity>`, so the Claude and Codex records coexist. The other ten single-identity install steps retain their bare function-name keys; one parameterized call site produces two runtime entries.
- Doctor REPAIR parses the full key, whitelists the base function, and invokes it with the key identity plus a fixed identity-to-mise-tool mapping. It no longer infers identity from recorded commands.
- Full parameterized keys flow unchanged through STALE messages and the remover. The existing generic jq lookup/deletion already handled colon keys, so no duplicate parser was added to the manifest library or remover.
- A missing installed manifest remains optional. A present unreadable, invalid-JSON, wrong-version, or non-object-steps manifest now emits exactly one `installed manifest unreadable or invalid: <path> (<reason>)` ERROR and skips both manifest-dependent scans.
- The remover's existing invalid-manifest exit 1 and exact full-key removal behavior are pinned with fake-HOME regression tests.

## Validation

- Regression-first: the new double-record, parameterized repair, and invalid-doctor tests failed before the implementation; the existing generic remover behavior already passed and is now pinned.
- Targeted fake-HOME transcript: five double-record/repair/doctor/remover tests passed.
- `make unit-test`: 304 tests passed.
- `make validate-agent-assets`: `agent asset validation ok`.
- `shellcheck -x` and `bash -n` on all three allowed shell paths: clean.
- `make format`: clean; `git diff --check`: clean.
- T61b-added PIE807 scope: clean. The optional whole-file Ruff audit still reports nine pre-existing accepted T57/T59 findings outside this task's requested gates; no broad reformat or mode change was made.
- Crit receipt gate uses resolved approval `r_b430f5`.

Durable project decision memory added successfully as `3e5bb65b-4548-4a5e-8197-b5445b24d6bb`:

```text
Parameterized install steps record per-identity manifest keys (function:identity) so no invocation overwrites a sibling's record, and an existing-but-unreadable installed-manifest is a doctor ERROR, never silently treated as empty.
```

[memory: decision — Parameterized install steps record per-identity manifest keys (function:identity) so no invocation overwrites a sibling's record, and an existing-but-unreadable installed-manifest is a doctor ERROR, never silently treated as empty.]

T61b writes are limited to allowed product/test paths and its five orchestration artifacts plus Crit evidence. Accepted shared-worktree changes were preserved. No Bats, chezmoi apply, dependency change, GitHub write, commit, or push occurred.
