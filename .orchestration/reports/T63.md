# T63 Report

Status: `ready_for_review`

`effects=none`

The model-selection and agmsg orchestration rules now require disposable E2E test-subject agent sessions to launch through the regulated `express` profile arguments from `~/.agents/model-profiles.env`.

## Update

- Added one bullet to `model-selection.md` naming `MODEL_PROFILE_EXPRESS_CLAUDE_ARGS` and `MODEL_PROFILE_EXPRESS_CODEX_ARGS` and retaining the prohibition on ad-hoc `--model` flags for throwaway sessions.
- Added one bullet beside the live-E2E orchestration clause applying the same rule to orchestrator-driven test-subject panes.
- Added no model ID, launcher behavior, generated-file change, or code change.

## Validation

- `make validate-agent-assets`: passed after rerunning outside the workspace sandbox so `uv` could access its existing cache.
- Full diff: exactly two added English bullets across the two allowed rule files.
- Rendered reference: `MODEL_PROFILE_EXPRESS_CLAUDE_ARGS="--model haiku --effort low"` and `MODEL_PROFILE_EXPRESS_CODEX_ARGS="--profile express"` confirm both variable names.
- `git diff --check`: passed.

## Project Memory

Recorded decision `4520ed3f-0eee-451f-b3ff-697e2040fbb1` with:

`python3 .claude/hooks/contextdb_cli.py memory add --kind decision --scope project --content "Disposable E2E test-subject agent sessions launch with the express profile args from ~/.agents/model-profiles.env (MODEL_PROFILE_EXPRESS_CLAUDE_ARGS / _CODEX_ARGS), never with ad-hoc --model flags; harness behavior under test is model-independent, so the cheapest profile is the regulated default."`

## Plan Quality

The validator/Make target, hook, reviewer definition, and CI entrypoint are absent. The manual fallback passed for exact scope, measured env evidence, positive/adversarial checks, and STOP conditions. Entry points were checked with:

`for artifact_path in docs/workflows/plan-quality-gate.md plans/AGENTS.md plans/TEMPLATE.md .claude/agents/plan-quality-reviewer.md .claude/hooks/plan-quality-gate.sh scripts/validate_plan_quality.py; do test -e "$artifact_path"; done; rg -n 'plan-quality|validate_plan_quality' Makefile .github`

No Bats, chezmoi apply, dependency change, commit, push, runtime mutation, or external effect occurred.
