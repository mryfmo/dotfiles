# T64b Report

Status: `ready_for_review`

`effects=none`

Codex- and Claude-facing guidance now agree that `/security-review`, permgate policy, redaction/secret handling, and trust-boundary audits use the `security` profile, while `deep` is reserved for non-audit cross-cutting design and unknown failures.

## Update

- Replaced the conflicting Codex `security -> deep` guidance with explicit `--profile security` routing for all four audit scenarios.
- Narrowed the Claude `deep` rule by removing its conflicting `security-sensitive work` clause.
- Left classifier pinning, permgate fail-closed behavior, adversarial review criteria, and trust-boundary safety requirements unchanged because they do not route models.

## Validation

- `make validate-agent-assets`: passed.
- Full two-surface security-routing audit: four post-fix routing clauses, two to `security` for audits and two to `deep` for non-audit escalation; no conflict remains.
- Full diff and `git diff --check`: passed; T64b changes exactly two guidance lines.

## Project Memory

Recorded decision `69c391ae-aa08-4a43-995f-8f4f6e3dbe8c` with the task-specified cross-surface routing fact.

## Review

Crit-data scope approval `r_790f61` is resolved in `.agents/worklog/codex/review/T64b-crit-comments.json`; no finding remains.

No code, model-profile, generated-file, Bats, chezmoi apply, dependency, commit, push, deployment, or external effect occurred.

