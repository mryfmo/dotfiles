# T65 Report

Status: `ready_for_review`

`effects=none`

The chezmoi-managed Pi skeleton now ships a global `settings.json` with `defaultProjectTrust: "never"`, an extensions placeholder, and a `pi assets` validator category. No Pi executable was installed or run.

## Upstream Trust Contract

Pinned `v0.84.2` defines accepted values as `"ask" | "always" | "never"` in [settings-manager.ts line 63](https://github.com/earendil-works/pi/blob/v0.84.2/packages/coding-agent/src/core/settings-manager.ts#L63).

The pinned security documentation states: “`defaultProjectTrust: "ask"` and `"never"` ignore such resources, while `"always"` trusts them.” See [security.md line 22](https://github.com/earendil-works/pi/blob/v0.84.2/packages/coding-agent/docs/security.md#L22). Therefore `"never"` is the explicit deny/untrusted baseline for non-interactive modes without a persisted decision.

## Update

- Added `home/dot_pi/agent/settings.json` with only the verified trust key.
- Added `home/dot_pi/agent/extensions/.gitkeep` for the T66/T69 extension sources.
- Added `validate_pi_assets`: when the skeleton exists it requires valid object JSON, exact `defaultProjectTrust: "never"`, and the extensions directory; independently, a present mise Pi entry must be exactly `0.84.2`.
- Added fixtures for a valid tree, missing trust, absent deferred pin, and a rejected version range.

## Spec Correction

The first implementation exposed that adding only the config pin violates the existing config-lock invariant while T65 forbade lock regeneration. The orchestrator corrected its spec: the attempted config line was removed, no lock-test exception was added, and the Pi config+lock+install change is deferred as one orchestrator-side chore before T67.

## Validation

- `make format`: passed.
- `make unit-test`: 312 passed.
- `make validate-agent-assets`: passed.
- Focused Pi tests: 4 passed after the original three demonstrated the missing category.
- JSON parse, whitespace, unchanged mise config, and unchanged mise lock checks: passed.

## Project Memory

Recorded corrected decision `127e89bb-0580-41e9-92b0-6fe77dfb432f` with the trust baseline and atomic config+lock defer.

## Review

Crit-data scope approval `r_3b8dc0` is resolved in `.agents/worklog/codex/review/T65-crit-comments.json`; no finding remains.

No Pi execution, install, dependency change, mise config/lock change, Bats, chezmoi apply, commit, push, deployment, or external effect occurred.

