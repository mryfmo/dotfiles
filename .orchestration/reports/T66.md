# T66 Report

Status: `ready_for_review`

`effects=none`

Pi now has a fail-closed `tool_call` permission extension backed by the existing deterministic permgate policy. The extension sends normalized action JSON on stdin to `permgate pi`; no Pi binary was executed or deployed.

## Pinned v0.84.2 Contract

- The loader imports the extension's default export, requires a function, and invokes it with the extension API: [loader.ts lines 455–508](https://github.com/earendil-works/pi/blob/v0.84.2/packages/coding-agent/src/core/extensions/loader.ts#L455-L508).
- The factory type is `(pi: ExtensionAPI) => void | Promise<void>`: [types.ts lines 1518–1519](https://github.com/earendil-works/pi/blob/v0.84.2/packages/coding-agent/src/core/extensions/types.ts#L1518-L1519).
- `tool_call` exposes bash/read/edit/write/grep/find/ls inputs and supports `{ block, reason }`: [types.ts lines 853–912](https://github.com/earendil-works/pi/blob/v0.84.2/packages/coding-agent/src/core/extensions/types.ts#L853-L912), [types.ts lines 1071–1074](https://github.com/earendil-works/pi/blob/v0.84.2/packages/coding-agent/src/core/extensions/types.ts#L1071-L1074).
- Pi distinguishes `tui`, `rpc`, `json`, and `print`; `hasUI` is true in TUI and RPC, so T66 explicitly permits confirmation only in `tui`: [types.ts lines 305–313](https://github.com/earendil-works/pi/blob/v0.84.2/packages/coding-agent/src/core/extensions/types.ts#L305-L313).

## Update

- Added a strict stdin JSON protocol for `permgate pi`: exact compact one-line allow/deny/ask output on success, nonzero on parse/config/decision/logging failures.
- Added a Pi policy section that references the shared deny-then-allow layers and pins `llm_enabled: false`; shared `decide` skips classifiers for Pi.
- Added the default-export Pi extension for bash/write/edit with 7000 ms timeout, TUI confirmation for ask, and one-line `blocked by policy` denial for every fail-closed path. Read/grep/find/ls never invoke permgate.
- Added SHA-256 validation for the exact managed extension bytes and matching/tamper fixtures.
- Added direct Node behavior tests, always-run `node --check`, and optional locally available `tsc --noEmit` without dependencies.

## Validation

- Test-first run: seven new failures while the legacy golden passed; after implementation the focused suite passed 40 tests.
- `make format`: passed.
- `make unit-test`: 322 passed.
- `make validate-agent-assets`: passed.
- `node --check` and local TypeScript 5.9.3 `tsc --noEmit`: passed.
- Exact Claude/Codex allow, deny, and native-ask stdout remains pinned byte-for-byte.

## Project Memory

Recorded decision `0f59309c-cbe8-4a0f-826b-df5854cbed69`.

## Review

Crit-data approval `r_8b57d3` is resolved in `.agents/worklog/codex/review/T66-crit-comments.json`. Review found one CI-portability defect—unconditional global `tsc` use—which was fixed with an always-run Node syntax fallback and optional compiler gate.

No Pi execution, deployment, dependency change, Bats, chezmoi apply, commit, push, network/state access from the extension, or external effect occurred. Live verification remains T72 E2E-π2.
