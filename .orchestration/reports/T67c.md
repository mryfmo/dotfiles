# T67c report: checker credential-lane precedence

Status: ready for review

## Result

Fixed the shared Pi model-access prerequisite selector at its root branch.
Every provider now follows one order: a present documented environment
variable wins; otherwise an exact provider entry in the read-only subscription
auth file enables the existing model checks; otherwise the checker safely
SKIPs with a reason naming both the environment and `/login` lanes.

The existing `subscription_auth_available` presence-only helper is reused. No
credential value is read into shell variables, printed, passed through argv, or
copied. Providers without an env mapping, including openai-codex, retain T67b
behavior as the natural fallback case.

The living T67 operator form's single coverage note now records the uniform
precedence. Its historical results remain unchanged.

## Validation and review

- Test-first red phase: both new/updated Anthropic cases failed on the old
  mapped-provider branch.
- Focused fake-Pi suite: 11 tests passed.
- Full unit suite: 343 tests passed.
- Format, Bash syntax, ShellCheck, agent asset validation, whitespace, scope,
  and Crit review gates passed.
- Crit record `r_9ef410` is resolved with no requested change.

No real Pi, real credential file/content, chezmoi apply, Bats, dependency
change, commit, or push occurred.

## Failure memory

[memory: failure — pi-model-access-check gap #2: providers WITH an env-key mapping (anthropic) never fell through to the subscription lane, so with ANTHROPIC_API_KEY unset but a valid auth.json subscription entry the checker SKIPped although a direct claude-fable-5 round trip succeeded. Lane precedence must be: env var set -> env lane; else auth.json entry -> subscription lane; else SKIP naming both.]

Recorded as `632cae23-6d57-4305-83db-a9e964cd20af`.

## Effects

effects=none

