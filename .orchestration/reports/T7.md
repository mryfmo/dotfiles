# T7 Report

## Status

ready_for_review

## Changes

- Moved login-shell PATH construction from `home/dot_zshrc` to `home/dot_zprofile`.
- Left interactive completion `fpath` setup in `home/dot_zshrc`.
- Updated `tests/unit/test_herdr_agents.py` with a static check that `.zprofile` adds `${HOME}/.local/bin/common` to `path` and `.zshrc` no longer owns the combined `path fpath` setup.

## Notes

- README did not describe where PATH is set, so it was not changed.
- Did not edit deployed `~/.zprofile` or `~/.zshrc`.
- Did not run forbidden actions: no commit, push, `chezmoi apply`, `make update`, `make require-crit-review`, bats, Herdr operations, or Ghostty restart.
