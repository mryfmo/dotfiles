# T74 report: Pi source removal and neutral CLI permission lane

Status: ready for review

## Result

The complete commit-derived pure-Pi source inventory is removed: 24 files
covering `home/dot_pi`, three installed-source executables, six Node extension
test/type assets, six Python test modules, and four session fixtures. Empty
source/fixture directories were removed. The mise pin and lock pair remain
byte-unchanged for the separately owned T75 chore; no deployed runtime was
touched.

The reusable permission protocol remains intact under the neutral `cli`
provider name. Only identifiers, protocol diagnostics, policy key, and matching
test names changed from `pi` to `cli`; workspace-write, strict realpath,
sensitive-read denial, deny precedence, deterministic allow reuse, fail-closed
behavior, compact JSON decisions, and Claude/Codex hook bytes are preserved.

Inventory §2b was applied exactly: Pi registration was removed from `join.sh`,
Pi documentation was removed from `whoami.sh`, bridge-only inbox environment
controls were removed while shdoc stayed, the Pi worker paragraph was removed
from the orchestration skill, and the Pi-assets validator category plus its 12
tests was deleted.

Inventory §2c shared assets are byte-unchanged: `send.sh`, its SQL regression,
the trusted-runtime Codex receiver, and its regression all pass
`git diff --exit-code`. No unclassified product hunk changed.

## Validation and count accounting

- Test-first `cli` protocol check failed before the implementation rename.
- Focused permgate/validator/agmsg suite: 77 tests passed.
- Full unit suite: 329 tests passed.
- The previous 382-test baseline shrank by exactly 53: 41 named tests from
  deleted pure-Pi Python modules plus 12 named Pi-assets validator tests.
- All 22 nested Node behavior tests, three TypeScript contracts, and four JSONL
  support fixtures are separately named in the validation artifact.
- `make format`, Bash syntax, ShellCheck, Python compile, policy JSON parse,
  `make validate-agent-assets`, `git diff --check`, residue, scope, and Crit
  gates passed.
- Crit approval `r_60db89` and the in-flight `run_pi` helper-name residue
  finding `r_5f8ece` are resolved; its receipt passes `make require-crit-review`.

## Residue classification

The raw source scan has zero unjustified third-agent references. Every
remaining source hit is one of:

- the T75-owned mise config/lock pin (three hits);
- the retained `.pi/agent/auth.json` sensitive-read protection and its tests
  (three hits), required because operator credentials remain by decision §2d;
- the proper project/protocol name `flue-pi` in five existing store/routing
  examples, unrelated to a Pi third-agent runtime.

Ignored generated `__pycache__` binaries also matched raw `grep`; they are not
source, are not tracked, and runtime teardown is outside T74.

## Decision memory

[memory: decision — Pi is removed as a third agent per operator ruling; the
permgate machine-readable lane survives renamed `pi` to `cli` with its
workspace-write, strict-resolution, and sensitive-read machinery intact, and
every shared-asset hunk remains unchanged.]

Recorded as `e1dbbefb-572a-4d97-a655-901ccc1b0a99` with:

```text
python3 .claude/hooks/contextdb_cli.py memory add --kind decision --scope project --content 'T74: Pi is removed as a third agent per operator ruling. The permgate machine-readable lane survives renamed pi to cli with workspace-write, strict-resolution, sensitive-read, and deny-precedence behavior intact. Shared send.sh SQL safety, trusted-runtime Codex receiver, and shdoc conversions remain unchanged.'
```

## Effects

effects=none
