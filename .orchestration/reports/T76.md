# T76 report: core agmsg identifier validation and acceptance cost line

## Result

[memory:decision] `send.sh` now validates `team`, `from`, and `to` against
`^[a-z0-9][a-z0-9_-]{0,63}$` before loading storage code or touching the DB.
The body remains unrestricted free text and continues through the existing
SQL-safe literal path. Acceptance records now require worker-reported token/
cost figures when available, otherwise `cost: n/a`.

## Precondition inventory

The supplied `whoami.sh` and `team.sh` interfaces enumerated nine teams and
all registrations without reading team configuration directly. Every team and
identity passed the exact grammar:

- `ai-ops-platform`: `claude-deep-dot`, `codex-deep-dot`
- `dotfiles-conformance`: `claude-deep-dot`, `codex-deep-dot`
- `enmaru-project`: `claude-lead`, `codex-impl`
- `flue-pi-productionization`: `claude-deep-flue`, `codex-deep-flue`
- `herddeck-project`: `claude-fable5high-herddeck`, `codex-gpt56solhigh-herddeck`
- `herness-self-improvement`: `claude-fable5high-herness`, `codex-gpt56solhigh-herness`
- `icsnet-project`: `claude-lead`, `codex-impl`
- `t20-scratch`: `t20-watcher`
- `towa-no-neko-project`: `claude-lead`, `codex-impl`

No legacy identity required a stop or migration.

## Changes

- Added one exact-regex loop before `SCRIPT_DIR`, storage sourcing, DB path
  resolution, initialization, and SQLite access.
- Reworked `test_agmsg_send.py` to cover 64-character success, 65-character
  rejection, first-character/case constraints, quote/space/metacharacter
  rejection for all three identifiers without storage creation, and a
  quote-bearing body round-trip.
- Added the specified acceptance `cost:` rule to the shared skill and Claude
  orchestration rule.

The malformed regex rendering in work-order item 1 was resolved from the
task's authoritative memory marker and requested title, both of which specify
`^[a-z0-9][a-z0-9_-]{0,63}$`.

## Validation

Focused RED failed in all nine initial invalid-identifier subtests because the
old script entered DB initialization. Focused GREEN passed 4 tests. The full
suite passed 331 tests; formatting, Bash syntax, ShellCheck, agent-asset
validation, diff whitespace, and Crit evidence gates passed. Full evidence is
in `.orchestration/validation/T76.txt`.

## Decision memory

Recorded as `dd27ee8e-27ef-4d69-9297-94439eca0c9e` with:

```text
python3 .claude/hooks/contextdb_cli.py memory add --kind decision --scope project --content 'T76: agmsg send.sh validates team, from, and to before any storage work against the exact grammar ^[a-z0-9][a-z0-9_-]{0,63}$ while the message body remains unrestricted free text and SQL-safe. AGMSG acceptance records include a cost line with worker-reported token/cost figures when available, otherwise cost: n/a.'
```

## Effects

effects=none
