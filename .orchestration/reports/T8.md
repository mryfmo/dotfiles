# T8 Report

## Status

ready_for_review

## Changes

- Normalized chezmoi source prefixes in `scripts/check-agent-runtime.py`:
  - `executable_foo` is compared against deployed `foo`;
  - `private_foo` is compared against deployed `foo`.
- Added executable-bit validation for deployed files whose source has the `executable_` prefix.
- Ignored agmsg runtime-state paths in shared skill tree comparisons:
  - `agmsg/.agmsg`
  - `agmsg/db/**`
  - `agmsg/run/**`
  - `agmsg/teams/**`
- Added unmanaged top-level shared-skill handling:
  - top-level deployed skill dirs absent from repo source print `WARN: unmanaged skill dir: ...`;
  - checker exit remains 0 when only those warnings are present;
  - strict missing/unexpected/content checks still apply inside repo-managed top-level skill dirs.
- Added `tests/unit/test_check_agent_runtime.py` covering prefix normalization, executable-bit failures, runtime ignores, unmanaged-skill warnings, and strict managed-subtree drift detection.

## Validation Summary

- `uv run scripts/check-agent-runtime.py; echo exit=$?`: exit 0, with warnings for unmanaged `crit`, `crit-cli`, `find-skills`, and `improve`.
- `uv run python -m unittest discover -s tests/unit -v`: pass, 80 tests.
- `python3 -m py_compile scripts/check-agent-runtime.py tests/unit/test_check_agent_runtime.py`: pass.

## Forbidden Actions

Did not run git commit, git push, chezmoi apply, make update/upgrade, make require-crit-review, bats, Herdr operations, deployed-home edits, or deletion outside the repo.
