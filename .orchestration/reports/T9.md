# T9 Report

## Status

ready_for_review

## Changes

- Removed Ghostty `initial-command` from `home/dot_config/ghostty/config`, so Ghostty starts at a normal zsh prompt.
- Changed `herdr-agents` to use left/right layout:
  - Codex starts with `--split right`.
  - Missing-Claude repair splits from the Codex pane with `--direction right` and swaps the new Claude pane left.
  - shdoc/usage text now describes Claude left of Codex.
- Updated Codex managed default model from `gpt-5.5` to `gpt-5.6-sol` in `home/dot_agents/agent-config.yaml`.
- Regenerated `home/.chezmoitemplates/codex-config-managed.toml` with `uv run --with pyyaml python scripts/generate-agent-configs.py`.
- Updated `tests/unit/test_herdr_agents.py` expected Herdr calls, model strings, and Ghostty config assertion.
- Updated the README Herdr/Ghostty section to describe bare `herdr` inside Ghostty starting the managed layout and returning to shell on exit.

## Generated Files

- `home/.chezmoitemplates/codex-config-managed.toml` was regenerated by the generator.
- No other generated adapter changed.

## Forbidden Actions

Did not run `git commit`, `git push`, `chezmoi apply`, `make update`, `make upgrade`, `make require-crit-review`, local bats, Herdr operations, Ghostty restart, or dependency changes.

