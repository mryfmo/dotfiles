# T36 sandbox record

OpenSandbox was not used. The task ran in the assigned repository workspace and wrote only under `.ua/` plus the specified orchestration artifacts. The one required analyzer-core build occurred in the installed Understand-Anything plugin location after command approval because its missing dist artifact blocked analysis.
