# T50 Sandbox Record

- Repository: `/Users/mryfmo/Workspace/dotfiles` (shared managed worktree)
- Unit, example, and performance fixtures used `tests.support.TempProject` under `/private/tmp/compactiondb-test-*` and were removed after each run.
- The 10,000-event performance fixture was populated in one transaction; fixture generation time was excluded from the recall wall-time measurement.
- `make clean` removed only vendor-local bytecode, SQLite runtime files, and the writer lock as required by T50.
- No source-tree database remains after final cleanup.
- No commit, push, `chezmoi apply`, Bats, dependency change, LLM call, network call, new embedding generation, or recall-side DB write was performed.
- Existing shared-worktree changes were preserved.

