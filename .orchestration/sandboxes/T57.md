# T57 Sandbox Record

- Repository: `/Users/mryfmo/Workspace/dotfiles`
- Product/test writes are confined to `scripts/update-agent-assets.sh`, new `scripts/lib/asset-manifest.sh`, new `tests/unit/test_asset_manifest.py`, and the existing updater fixture in `tests/unit/test_runtime_health.py`.
- Fake-HOME validation used `/private/tmp/t57-fake-home.lrcNrT`; `rsync` and `herdr` were shell-function stubs and direct Homebrew `jq` avoided fake-HOME mise trust resolution.
- The fake manifest is mode `0600`; no file under the real HOME was installed, changed, or removed.
- The root CompactionDB database was written only by the explicitly required project decision command.
- Existing accepted T54/T55/T56 changes were preserved without modification.
- No commit, push, `chezmoi apply`, Bats run, dependency change, `make update`, network install, or real-HOME install step occurred.
