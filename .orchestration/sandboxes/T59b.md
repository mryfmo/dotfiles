# T59b Sandbox Record

- Repository: `/Users/mryfmo/Workspace/dotfiles`.
- Product/test writes are confined to `scripts/check-agent-runtime.py` and `tests/unit/test_check_agent_runtime.py`.
- Fake repair execution was confined to `/private/tmp/t59b_fake_home.py` and a `t59b-fake-home-*` temporary directory. The stub copied only the fake source `history.sh`; it never launched chezmoi.
- The three legacy database filenames were created only as ordinary text fixtures under fake HOME. The live stray `~/.agents/skills/agmsg/messages.db` and its sidecars were never opened, stat-mutated, moved, or removed by T59b.
- uv caches were confined to `/private/tmp`; existing cached PyYAML was reused for validation with no dependency change.
- Crit process evidence is repo-local under `.orchestration/validation/`; the required project CompactionDB failure record was added through the prescribed CLI.
- `effects=none`: T59b created no persistent product effect outside the repository working tree.
- No Bats, real `chezmoi apply`, dependency edit, guard weakening, commit, push, real-HOME repair, global install, or external service registration occurred.
