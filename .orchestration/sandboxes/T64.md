# T64 Sandbox

- Worker: `codex-deep-dot`
- Writes were limited to T64 allowed source/test/generated files, the five T64 artifacts, and ignored worklog/review evidence.
- The generator alone wrote `home/dot_agents/model-profiles.env` and `home/dot_codex/modify_security.config.toml`; the generated files were not hand-edited.
- Pre-existing accepted T63 rule and artifact changes were preserved without modification.
- The first render-proof command exposed a fake-HOME interaction with the mise shim and a sandboxed uv cache; rerunning the same proof with the installed Python interpreter isolated HOME to the modifier and passed.
- No Bats, chezmoi apply, dependency change, install, commit, push, or deployment occurred.
- `effects=none`

