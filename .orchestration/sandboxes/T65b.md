# T65b sandbox record

- Execution surface: managed `workspace-write` repository sandbox.
- Source verification: immutable v0.84.1 and v0.84.2 upstream text was fetched
  read-only into `/tmp` and mechanically compared; no repository dependency was
  added.
- Test isolation: the model-access suite used its existing temporary fake
  `HOME`, fake `PATH/pi`, fake extension, and invocation log.
- Real runtime boundary: no real Pi, provider authentication, model request,
  dependency installation, chezmoi apply, Bats, commit, or push occurred.
- Persistent external side effects: none.

