# T67 sandbox record

- Execution surface: managed `workspace-write` repository sandbox.
- OpenSandbox: unavailable in this worker session; no substitute remote or
  privileged sandbox was created.
- Test isolation: each fake-Pi test used a private `TemporaryDirectory` with a
  fake `HOME`, fake `PATH/pi`, fake extension file, and invocation log. Cleanup
  was automatic.
- External reads: pinned Pi v0.84.2 documentation/source was fetched read-only
  for protocol and extension-loader verification.
- Real runtime boundary: no real Pi process, subscription authentication,
  provider request, dependency installation, or user-HOME deployment occurred.
- Persistent side effects: none.
