# T67b sandbox record

- Execution surface: managed `workspace-write` repository sandbox.
- Credential boundary: the real `~/.pi/agent/auth.json` was never read. Tests
  created private temporary fake-HOME auth files containing sentinel-only
  credentials and asserted those sentinels absent from output.
- Runtime isolation: all model calls used the existing fake `PATH/pi`; no real
  Pi, provider authentication, network model request, or deployed extension ran.
- External reads: only official immutable Pi v0.84.1 `auth-storage.ts` source
  was read to verify the provider-keyed JSON layout.
- Persistent external side effects: none.

