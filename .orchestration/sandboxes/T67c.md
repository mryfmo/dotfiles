# T67c sandbox record

- Execution surface: managed `workspace-write` repository sandbox.
- Credential boundary: the real `~/.pi/agent/auth.json` was never read. Tests
  used temporary fake-HOME JSON containing sentinel-only values and asserted
  those sentinels absent from stdout and stderr.
- Runtime isolation: all model invocations used the existing fake `PATH/pi`;
  no real Pi, provider authentication, or network model request ran.
- Persistent external side effects: none.

