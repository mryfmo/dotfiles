# T67d sandbox record

- Execution surface: managed `workspace-write` repository sandbox.
- Credential boundary: the real `~/.pi/agent/auth.json` was never read; unit
  tests used only their temporary fake HOME and fake Pi executable.
- Runtime isolation: no real Pi, provider authentication, or network model
  request ran.
- `uv run` initially could not initialize `/Users/mryfmo/.cache/uv` inside the
  sandbox; the same approved `uv run` command was rerun with managed
  escalation. It performed no dependency change.
- Persistent external side effects: none.

