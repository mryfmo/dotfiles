# T67e sandbox record

- Execution surface: managed `workspace-write` repository sandbox.
- Credential boundary: the real `~/.pi/agent/auth.json` was never read; tests
  used only temporary fake HOME state and a fake Pi executable.
- Runtime isolation: no real Pi, provider authentication, or network model
  request ran.
- The pinned upstream source fetch required the approved read-only `curl -sS`
  escalation because sandbox DNS was unavailable. No repository or external
  state was mutated by that fetch.
- `uv` and validation gates used approved managed escalation for the existing
  cache outside the writable repository; dependencies were unchanged.
- Persistent external side effects: none.

