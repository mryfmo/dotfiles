# T68 sandbox record

- Execution surface: managed `workspace-write` repository sandbox.
- Runtime isolation: every Pi invocation used a temporary fake `PATH/pi`, fake
  HOME, fake extension, and fake agmsg scripts. The real Pi binary and deployed
  permgate extension were not executed.
- Filesystem guard: tests prove a canonical project outside the explicit
  scratch root fails before identity lookup or child startup.
- Transport isolation: fake SQLite stores and send spies exercised identity,
  team, cooldown, PING, and child-death behavior without external delivery.
- External evidence: the already-vendored local snapshot of official immutable
  Pi v0.84.1 extension documentation was read to confirm global auto-discovery.
- Persistent external side effects: none.

