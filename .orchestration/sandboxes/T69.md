# T69 sandbox record

- Execution surface: managed `workspace-write` repository sandbox.
- Runtime isolation: direct Node handlers used injected child-process and file
  existence stubs; no real Pi or model/provider process ran.
- Network boundary: the extension imports only Node builtins and the Pi virtual
  type module. It has no network call. A separate approved read-only source
  fetch retrieved pinned v0.84.1 type lines for citation only.
- State boundary: the extension writes no files; the tool-argument bridge is an
  in-memory map cleared at session start.
- `uv`, TypeScript, and validation commands used existing local tools/caches;
  no dependency was added.
- Persistent external side effects: none.

