# Sandbox
Dedicated git worktree and temporary HOME/storage/fake CLI fixtures. OpenSandbox not available; local isolated tests are the fallback. Escalation permitted uv cache access, unit tests and local Crit metadata only. No actual worker wakes, local Bats, operator-home apply, commits or pushes.

## PR173 revision
Existing worktree and temporary HOME/SQLite/fake herdr/send only. No actual pane mutation, external asset edits, commits or pushes. Full unit suite uses isolated fixtures.
