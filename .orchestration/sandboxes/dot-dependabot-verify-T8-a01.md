# T8 sandbox record

Read-only shell and GitHub API investigation in canonical repository. No OpenSandbox needed: no untrusted action executed locally. Nix missing (command -v nix exit 1); no worktree created, so none to remove. Writes limited to required orchestration artifacts, ignored worker worklogs and required local CompactionDB decision. No global installation, GitHub mutation, tracked-file edit or Bats execution.

