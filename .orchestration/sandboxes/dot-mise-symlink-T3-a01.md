# Sandbox

Existing workspace-write sandbox and dedicated worktree used. Only the validator retry needed approved escalation for uv cache. Native mise discovery was read-only; optional cache/tracking writes were denied but discovery succeeded. ChezMoi apply ran only against generated temporary source/destination and persistent-state paths, never operator HOME. No OpenSandbox was configured; local fixtures provide isolation.

## PR172 revision
All upgrade/apply checks used temporary fixture HOME and fake chezmoi. No real operator-home apply, Docker/VM or Bats. Existing worktree only; no commit/push.
