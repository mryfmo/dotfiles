# dot-residuals-T1-a01 sandbox record

OpenSandbox was not used. Hermetic unit tests used temporary HOME directories on the host. The requested public-only shell E2E used only `limactl shell adh-test`, created no credentials or login session, and removed both scratch users and home directories before completion.
