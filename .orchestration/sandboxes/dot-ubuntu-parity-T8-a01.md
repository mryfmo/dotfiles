# dot-ubuntu-parity-T8-a01 — sandbox / OpenSandbox status

Not used. One line reordered in an existing install script, one line
removed from a test, verified with `bash -n`, one targeted `bats -f` run
(mock-based, no real system mutation), and the repo's own local
unit-test/validate-agent-assets commands — no sandboxed execution needed.
