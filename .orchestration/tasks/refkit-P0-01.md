# AGMSG-TASK refkit-P0-01: add the `remediation` model profile

## Objective
Add a `remediation` entry to `model_profiles` in `home/dot_agents/agent-config.yaml` so the references-kit remediation orchestrator can be launched with Claude Fable 5.1 at effort medium without ad-hoc `--model` flags. Regenerate every rendered view of the manifest that lives in the repository.

## Context
- Plan: `.agents/worklog/claude/ai-references-vivid-sparrow.md` (second part, task P0-01) in the main worktree `/home/moriya/Workspace/dotfiles`.
- Rule: interactive model IDs and efforts live only in `model_profiles`; rendered files (`home/dot_agents/model-profiles.env`, any `profiles/model_profiles.json` validation view, Claude settings templates, `~/.codex/<profile>.config.toml` templates) are generated by `scripts/generate-agent-configs.py`. Never hand-edit a generated file.
- Existing profiles: `express`, `standard`, `review`, `deep`, `security`, `adh`. `adh` is pinned by a validator; do not touch it.

## Scope (allowed_files)
- `home/dot_agents/agent-config.yaml`
- every file that `scripts/generate-agent-configs.py` writes (discover them by reading the script; typical: `home/dot_agents/model-profiles.env`, `home/dot_agents/profiles/model_profiles.json` or similar, codex config templates under `home/.chezmoitemplates/`, Claude settings templates)
- `.orchestration/reports/refkit-P0-01.md`, `.orchestration/validation/refkit-P0-01.md`, `.orchestration/sandboxes/refkit-P0-01.md`, `.orchestration/learning/refkit-P0-01.md`, `.orchestration/autoskill/runs/refkit-P0-01.md`

## Required change
```yaml
  remediation:
    # references-kit remediation orchestrator: Fable 5.1 at reduced effort;
    # workers stay on `standard`.
    claude: { model: claude-fable-5-1, effort: medium }
    codex:
      model: gpt-5.6-terra
      model_reasoning_effort: medium
      notify: ['{{ .chezmoi.homeDir }}/.local/bin/common/contextdb-codex-notify']
```
Place it after `security` and before the `adh` comment block. Keep the codex half identical to `standard` (same model, effort, notify) unless the generator or validator requires otherwise; if it does, follow the validator and explain in the report.

## Steps
1. Read `scripts/generate-agent-configs.py` and `scripts/validate-agent-assets.py` to learn every output path and every invariant (e.g. profile name pattern, required keys, forbidden fallback/effort rules).
2. Edit the manifest.
3. Run the generator exactly as the repository does (check `Makefile` targets `update`/`apply`/`format` and the chezmoi scripts for the canonical invocation; use `uv run`). Do NOT run `chezmoi apply` and do NOT write under `$HOME`.
4. Run `make validate-agent-assets` and `make unit-test`.
5. Confirm with `grep -n REMEDIATION home/dot_agents/model-profiles.env` that `MODEL_PROFILE_REMEDIATION_CLAUDE_ARGS="--model claude-fable-5-1 --effort medium"` and `MODEL_PROFILE_REMEDIATION_CODEX_ARGS="--profile remediation"` are rendered.
6. `git add` only files in scope and commit on the current branch `feat/references-kit-v4` with message:
   `feat(agent-config): add the remediation model profile` + body explaining why, ending with `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`.
7. Record `[memory:decision] remediation profile = claude-fable-5-1/medium for the references-kit v4 remediation orchestrator; workers stay on standard` via `python3 .claude/hooks/contextdb_cli.py memory add --kind decision --scope project --content "..."` and paste the command and its output in the validation file.

## Forbidden actions
push; pr-create; chezmoi-apply; home-writes; editing `adh` profile; editing generated files by hand; dependency changes; touching `references/`; commits on `main`.

## Validation file must contain verbatim output of
`uv run python scripts/generate-agent-configs.py …` (actual invocation), `make validate-agent-assets`, `make unit-test`, the grep in step 5, `git show --stat HEAD`, and the contextdb command.

## Report must contain
files changed with reasons, the exact generator invocation you used and where you found it, any validator rule you had to satisfy, the commit hash, `cost:` line.

max_turns=20
