# T42 validation

The new rule includes `mechanical batch commit`, `no per-task artifact set`, `agmsg ACCEPTANCE history`, and `no untracked tail`. The previous `converged one-task tail` wording is absent from the rule file.
