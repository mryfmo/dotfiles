T46 validation
==============

1. Test-first behavior check
----------------------------

Command (vendor/compactiondb cwd):
  uv run --no-project python -m unittest tests.test_config -v

Pre-implementation result:
  Ran 4 tests in 0.015s
  FAILED (failures=2)

Expected failures:
  - missing recovery budgets still returned the old max_chars value 8500
  - files_budget_chars had no type validation

The explicit-override and unknown-key-preservation tests already passed, confirming existing recursive-merge behavior.

Post-implementation result:
  Ran 4 tests in 0.015s
  OK

2. Full vendor tests
--------------------

Command:
  make -C vendor/compactiondb test

Summary:
  Ran 51 tests in 3.457s
  OK

New tests: 4 in tests/test_config.py.

3. Release validation
---------------------

Command:
  make -C vendor/compactiondb validate

Final summary:
  status: pass
  passed: 10
  failed: 0
  unittest_suite: 51 tests passed with ResourceWarning promoted to error
  installed_project_smoke: pass (2 events)
  release_tree_clean: pass
  claude_code_executable: 2.1.232 (optional, pass)

The first validation attempt found project `__pycache__` artifacts generated by test execution. After removing those exact generated directories, a second attempt found only `.venv/lib/python3.11/site-packages/__pycache__`; that cache alone was removed while preserving `.venv`. The final required command passed all checks.

4. Manifest regeneration and integrity
--------------------------------------

Regeneration command (vendor/compactiondb cwd):

  t46_manifest_tmp=$(mktemp)
  {
    cut -c67- MANIFEST.sha256
    printf '%s\n' './tests/test_config.py'
  } | LC_ALL=C sort -u | while IFS= read -r t46_path; do
    shasum -a 256 "$t46_path"
  done > "$t46_manifest_tmp"
  mv "$t46_manifest_tmp" MANIFEST.sha256

This derives paths from the existing manifest, adds the genuinely new test in sort order, preserves formatting and ordering, and recalculates hashes.

Integrity command (vendor/compactiondb cwd):
  shasum -a 256 -c MANIFEST.sha256

Result:
  all 58 listed files: OK
  final line: ./validate.py: OK

A verification attempt from the repository root failed because manifest paths are package-root relative. It was rerun from the correct vendor cwd as shown above and passed; no hash mismatch remained.

5. Lint and whitespace
----------------------

Command (vendor/compactiondb cwd):
  uvx ruff check .claude/contextdb/contextdb/config.py tests/test_config.py

Result:
  All checks passed!

Command:
  git diff --check -- vendor/compactiondb/.claude/contextdb/contextdb/config.py vendor/compactiondb/.claude/contextdb/config.json vendor/compactiondb/docs/OPERATIONS.md vendor/compactiondb/CHANGELOG.md vendor/compactiondb/MANIFEST.sha256

Result: exit 0, no output.

A root-cwd Ruff retry hit uv cache sandbox permissions before lint initialized; the same targeted command from the vendor cwd passed as recorded above.

6. git status and task scope
----------------------------

Command: git status --porcelain

Output before T46 artifacts were written:
   M .orchestration/tasks/PLAN-compactiondb-research-integration.md
   M home/dot_mise/config.toml
   M home/dot_mise/mise.lock
   M vendor/compactiondb/.claude/contextdb/config.json
   M vendor/compactiondb/.claude/contextdb/contextdb/config.py
   M vendor/compactiondb/CHANGELOG.md
   M vendor/compactiondb/MANIFEST.sha256
   M vendor/compactiondb/docs/OPERATIONS.md
  ?? .orchestration/tasks/T46-compactiondb-recovery-config.md
  ?? vendor/compactiondb/tests/test_config.py

The plan/task inputs were authored or updated by the orchestrator. The mise changes predated T46. This worker did not edit those paths.

Command: git diff --stat

Output:
   .../PLAN-compactiondb-research-integration.md      |  6 +-
   home/dot_mise/config.toml                          |  8 +--
   home/dot_mise/mise.lock                            | 72 +++++++++++-----------
   vendor/compactiondb/.claude/contextdb/config.json  |  3 +-
   .../.claude/contextdb/contextdb/config.py          | 11 ++--
   vendor/compactiondb/CHANGELOG.md                   |  5 ++
   vendor/compactiondb/MANIFEST.sha256                |  9 +--
   vendor/compactiondb/docs/OPERATIONS.md             | 14 +++++
   8 files changed, 75 insertions(+), 53 deletions(-)

T46-attributable files are exactly the six allowed vendor paths below plus the five artifact paths:
  vendor/compactiondb/.claude/contextdb/contextdb/config.py
  vendor/compactiondb/.claude/contextdb/config.json
  vendor/compactiondb/tests/test_config.py
  vendor/compactiondb/docs/OPERATIONS.md
  vendor/compactiondb/CHANGELOG.md
  vendor/compactiondb/MANIFEST.sha256

No recovery.py, hook, CLI, dependency, redaction, capture, or memory semantics changed.

7. Full config diff
-------------------

Command:
  git diff -- vendor/compactiondb/.claude/contextdb/contextdb/config.py vendor/compactiondb/.claude/contextdb/config.json

```diff
diff --git a/vendor/compactiondb/.claude/contextdb/config.json b/vendor/compactiondb/.claude/contextdb/config.json
index f705d6b..3419690 100644
--- a/vendor/compactiondb/.claude/contextdb/config.json
+++ b/vendor/compactiondb/.claude/contextdb/config.json
@@ -51,7 +51,8 @@
     "context_items": 24
   },
   "recovery": {
-    "max_chars": 8500,
+    "max_chars": 12000,
+    "files_budget_chars": 2000,
     "recent_events": 12,
     "recent_prompts": 4,
     "recent_files": 12,
diff --git a/vendor/compactiondb/.claude/contextdb/contextdb/config.py b/vendor/compactiondb/.claude/contextdb/contextdb/config.py
index 2178763..f3e8337 100644
--- a/vendor/compactiondb/.claude/contextdb/contextdb/config.py
+++ b/vendor/compactiondb/.claude/contextdb/contextdb/config.py
@@ -8,7 +8,6 @@ from typing import Any
 from .paths import ProjectPaths
 from .util import atomic_write_text, pretty_json, safe_chmod
 
-
 DEFAULT_CONFIG: dict[str, Any] = {
@@ -56,7 +55,8 @@ DEFAULT_CONFIG: dict[str, Any] = {
         "context_items": 24,
     },
     "recovery": {
-        "max_chars": 8500,
+        "max_chars": 12000,
+        "files_budget_chars": 2000,
         "recent_events": 12,
         "recent_prompts": 4,
         "recent_files": 12,
@@ -86,15 +86,15 @@ def _merge(base: dict[str, Any], override: dict[str, Any]) -> dict[str, Any]:
     return result
 
 
-
-
 def _require_int(config: dict[str, Any], section: str, key: str, *, minimum: int) -> None:
     value = config.get(section, {}).get(key)
     if isinstance(value, bool) or not isinstance(value, int) or value < minimum:
         raise ValueError(f"ContextDB config {section}.{key} must be an integer >= {minimum}")
 
 
-def _require_number(config: dict[str, Any], section: str, key: str, *, minimum: float, maximum: float | None = None) -> None:
+def _require_number(
+    config: dict[str, Any], section: str, key: str, *, minimum: float, maximum: float | None = None
+) -> None:
     value = config.get(section, {}).get(key)
     if isinstance(value, bool) or not isinstance(value, (int, float)) or float(value) < minimum:
         raise ValueError(f"ContextDB config {section}.{key} must be a number >= {minimum}")
@@ -124,6 +124,7 @@ def validate_config(config: dict[str, Any]) -> dict[str, Any]:
     _require_int(config, "memory", "context_items", minimum=1)
     _require_int(config, "recovery", "max_chars", minimum=1000)
+    _require_int(config, "recovery", "files_budget_chars", minimum=0)
     for key in ("recent_events", "recent_prompts", "recent_files", "recent_failures"):
         _require_int(config, "recovery", key, minimum=0)
     semantic = config.get("semantic", {})
```

8. Forbidden actions
--------------------

No git commit, git push, chezmoi apply, bats, dependency change, recovery.py change, hook/CLI behavior change, or unrelated config-semantic change was performed.
