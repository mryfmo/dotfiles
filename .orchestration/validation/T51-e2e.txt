T51 live E2E evidence
=====================

E2E-1: recovery packet injection (PASS) — 2026-08-16
----------------------------------------------------
Setup: fresh scratch project ~/Workspace/compactiondb-e2e1, compactiondb-install
from the updated ~/.agents/compactiondb (T46/T47/T48b/T49/T50 runtime),
real Claude Code session (haiku driver) in a herdr pane.

Steps: created cache.py (Write), extended it (Edit), verified via Bash,
then /compact (PreCompact manual + PostCompact captured in ledger).

Verification: asked the post-compaction session to quote the injected
recovery block. The model quoted the fixed section order:
  Goal / File modifications / Recent activity / Decisions / Open tasks /
  Failures / Compact summary
and the File modifications body verbatim:
  cache.py (edit, 2x)
  .agents/worklog/claude/create-a-file-named-floofy-raccoon.md (write, 1x)
This matches the T47 deterministic artifact-trail format (last op = edit,
write/edit count = 2) generated by a REAL compaction flow, not a fixture.

Ledger rows observed (scratch DB):
  pre_compact  | PreCompact (manual)
  post_compact | PostCompact: <analysis> ...
  tool_success | Write/Edit succeeded: cache.py
  turn_stop    | Stop: Done. Created cache.py ...

E2E-2: codex notify ingest (PASS) — fresh + persisted-restore
--------------------------------------------------------------
Precondition: T48 defect found at deployment (modify_ scripts are executed,
not templated, so the notify path arrived as a literal
'{{ .chezmoi.homeDir }}/...') was fixed by T48c (apply-time Path.home()
expansion) and the corrected configs were applied:
  ~/.codex/{standard,deep}.config.toml notify = ["/Users/mryfmo/.local/bin/common/contextdb-codex-notify"]

Fresh session: real Codex (gpt-5.6-sol high, --profile deep) in the
opted-in scratch project; one read-only turn; after turn completion the
notify chain produced ledger row:
  18 | codex | {"client":"codex-tui","cwd":"/Users/mryfmo/Workspace/compactiondb-e2e1...
Persisted restore: session exited, resumed with
`codex --profile deep resume --last`, one turn -> second row:
  19 | codex | {"client":"codex-tui",...
Spool and quarantine empty; no health errors.

Operational finding (recorded, not a defect): `codex resume --last` WITHOUT
the --profile flag loads no profile config and therefore no notify — a
resumed worker must always be relaunched with its profile flag.

Known minor (accepted at T48): raw notify payloads normalize to
event_type='unknown' with the JSON as summary; turn-level attribution via
ingested_from='codex' is the contracted evidence.

E2E-3: AGMSG marker-contract round trip (PASS)
----------------------------------------------
T46-T50 RESULTs each carried [memory: decision — ...] markers per the T45
contract and were verified at acceptance (see .orchestration/acceptance/).
Repo is not opted in, so memory add was correctly skipped; the opted-in
command path is exercised by E2E-1/E2E-2 ledgers instead.
